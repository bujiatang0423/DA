# DA 未完成任务跟踪清单

> 项目：四维盾剑 V2.12 混合量化研究平台  
> 更新时间：2026-07-17  
> 用途：作为 DA 主服务、`DA-worktrees` 智能体和后续执行计划的共同入口。

## 使用规则

- `[ ]`：未完成，或当前服务尚未达到验收标准。
- `[x]`：已完成，并且当前主服务已有代码/测试/迁移等证据。
- 每个任务只在对应验收标准全部满足后勾选；“已有基础版本”不等于完成。
- 实现必须继续复用 `V212StrategyEngine`、PIT 输入链和现有计划，不在功能模块内复制公式。
- `auto_trade_enabled` 永远为 `false`；所有交易建议都必须人工确认。
- 数据、政策、财报或 LLM 因子缺失时必须 fail-closed，不得生成可执行候选。
- DA 运行时不得依赖 LA 的代码、数据库、工作目录或未冻结文件。

## 当前服务快照

### 已有可验证成果

- 候选结果已有 PostgreSQL 持久化、幂等保存、结果 API、worker handler 和 Web 轮询。
- 候选流程已经能完成 `queued → succeeded`；数据源不可用时返回空结果和质量码，不伪造推荐。
- 当前组合根仍使用 `UnavailableResearchWarehouse`，所以还不能生成真实市场候选。
- 持仓维护页面和快照投影已可用；持仓分析仍是早期基础版本。
- 历史回测基础引擎和页面存在，但严格 PIT 回测和真实 worker 接线未完成。
- 历史持仓 CLI、原始文件冻结和 opening positions 底层代码存在；Web 导入尚未完成。
- 当前测试基线：后端非 PostgreSQL 测试已通过，前端基础 Vitest/typecheck/build 已通过；完整 PostgreSQL、Playwright 和 Docker 验收未完成。

### 相关 Worktree 当前进度

| Worktree | 分支 | 当前最后提交 | 结论 |
| --- | --- | --- | --- |
| `DA-worktrees/00-foundation` | `codex/00-foundation` | `d899ad0` | 基础契约、策略核心、运行队列基础已完成；系统集成未完成 |
| `DA-worktrees/01-pit-legacy` | `codex/01-pit-legacy` | `6f5328b` | PIT/legacy 底层已完成一部分；provider 组合和验收未完成 |
| `DA-worktrees/02-candidates` | `codex/02-candidates` | `257e530` | 候选模型、生命周期、投影完成；Task 4 以后未完成 |
| `DA-worktrees/03-holdings` | `codex/03-holdings` | `fa5d27d` | 持仓分析模型/投影有实现；完整服务、持久化和 worker 未完成 |
| `DA-worktrees/04-backtest` | `codex/04-backtest-research` | `62556ab` | research 回测基础实现；严格 PIT 未完成 |
| `DA-worktrees/05-backtest-execution` | `codex/05-backtest-execution` | `5299a3f` | 回测执行闭环有实现；系统组合和 PIT 证书未完成 |

## P0：接通真实候选推荐生产链

对应计划：

- [计划 02：候选推荐](docs/superpowers/plans/2026-07-16-02-candidate-recommendation.md)
- [计划 06：系统集成](docs/superpowers/plans/2026-07-16-06-system-integration.md)

### 候选功能垂直切片

- [x] **Candidate Task 1：定义候选结果模型**  
  计划位置：计划 02 `### Task 1`。当前文件：`backend/app/features/candidates/models.py`。
- [x] **Candidate Task 2：候选生命周期状态机**  
  计划位置：计划 02 `### Task 2`。当前文件：`backend/app/features/candidates/state_machine.py`。
- [x] **Candidate Task 3：共享策略结果投影**  
  计划位置：计划 02 `### Task 3`。当前文件：`backend/app/features/candidates/strategy_projection.py`。
- [x] **Candidate Task 4：候选结果持久化和幂等保存**  
  计划位置：计划 02 `### Task 4`。当前文件：`repository.py`；迁移：`backend/migrations/versions/20260717_0004_candidate_results.py`。
- [x] **Candidate Task 5：CandidateService 点时编排**  
  计划位置：计划 02 `### Task 5`。当前文件：`backend/app/features/candidates/service.py`。
- [x] **Candidate Task 6：结构化结果到 Markdown 的单向投影**  
  计划位置：计划 02 `### Task 6`。当前文件：`backend/app/features/candidates/markdown.py`。
- [x] **Candidate Task 7：候选 API 和 worker handler**  
  计划位置：计划 02 `### Task 7`。当前文件：`contracts.py`、`router.py`、`jobs.py`、`module.py`。
- [x] **Candidate Task 8：候选 Web 提交、轮询和结果展示**  
  计划位置：计划 02 `### Task 8`。当前目录：`web/src/features/candidates/`。
- [ ] **Candidate Task 9：候选功能完整不变量和集成交接**  
  计划位置：计划 02 `### Task 9`。尚缺：真实 provider、完整 fake fixture、PostgreSQL 生命周期测试、结果分组和证据链验收。

### 真实数据组合根

- [ ] **Candidate Integration Task 1：统一组合根**  
  计划位置：计划 06 `### Task 1`。当前 `backend/app/bootstrap/composition.py` 仍注入 `UnavailableResearchWarehouse`，没有接入可用的点时数据仓库。
- [ ] **Candidate Integration Task 2：市场行情 provider**  
  计划位置：计划 01 `### Task 3`、计划 06 `### Task 1`。AkShare/BaoStock 适配器文件存在，但未接入统一生产组合根。
- [ ] **Candidate Integration Task 3：政策、财报、LLM 因子 provider**  
  计划位置：计划 01 `### Task 4`；当前只有部分 adapter/port，尚未形成完整采集链和生产配置。
- [ ] **Candidate Integration Task 4：候选真实推荐验收**  
  计划位置：计划 06 `### Task 4`。必须证明真实数据下能产生可执行、观察、排除三类结果；数据缺失只能 fail-closed。
- [ ] **Candidate Integration Task 5：幂等重放和跨重启验收**  
  计划位置：计划 06 `### Task 4`。必须证明相同幂等键不重复任务/结果，API 或 worker 重启后结果仍可查询。

## P0：完成持仓分析 V2.12 链路

对应计划：[计划 03：持仓分析](docs/superpowers/plans/2026-07-16-03-holding-analysis.md)

- [x] **Holding Task 1：持仓建议模型和基础原因码**  
  计划位置：计划 03 `### Task 1`；当前已有 `backend/app/features/holdings/models.py` 等投影文件。
- [x] **Holding Task 2：动作优先级和 T+1 基础规则**  
  计划位置：计划 03 `### Task 2`；当前已有 `priority.py`，但仍需集成验收。
- [x] **Holding Task 3：加仓条件和成本保护基础逻辑**  
  计划位置：计划 03 `### Task 3`；当前已有 `addition.py`，尚未接入完整服务链。
- [x] **Holding Task 4：共享事实到持仓建议投影**  
  计划位置：计划 03 `### Task 4`；当前已有 `strategy_projection.py`。
- [ ] **Holding Task 5：持仓分析结果持久化和幂等**  
  计划位置：计划 03 `### Task 5`；当前没有与候选结果等价的持仓分析结果 repository/迁移。
- [ ] **Holding Task 6：点时持仓分析编排服务**  
  计划位置：计划 03 `### Task 6`；当前 `HoldingAnalysisService` 仍是基础快照分析，不完整调用 PIT + V212 链路。
- [ ] **Holding Task 7：结构化持仓 Markdown 投影**  
  计划位置：计划 03 `### Task 7`；已有基础 markdown 文件，但未达到计划中的数据等级、证据链和审计要求。
- [ ] **Holding Task 8：持仓查询 API、异步任务和 worker handler**  
  计划位置：计划 03 `### Task 8`、计划 06 `### Task 4`；当前 worker 只注册候选 handler。
- [ ] **Holding Task 9：持仓 Web 完整结果页**  
  计划位置：计划 03 `### Task 9`；持仓维护页已完成，但 V2.12 分析结果页、证据和风险码展示未完成。
- [ ] **Holding Task 10：持仓不变量和集成交接**  
  计划位置：计划 03 `### Task 10`。

## P0：完成真实业务 worker 接线

对应计划：计划 00 `### Task 9-11`、计划 06 `### Task 1` 和 `### Task 4`。

- [x] **Worker Task 1：数据库队列、幂等和 SKIP LOCKED 领取基础**  
  当前文件：`backend/app/features/runs/repository.py`、`runs/service.py`。
- [x] **Worker Task 2：handler registry、基础 heartbeat/progress 和恢复接口**  
  当前文件：`backend/app/infrastructure/tasks/handlers.py`、`worker.py`。
- [x] **Worker Task 3：候选推荐 handler 注册**  
  当前文件：`backend/app/worker_main.py`；仅候选已注册。
- [ ] **Worker Task 4：持仓分析 handler 注册**  
  计划位置：计划 06 `### Task 1`、`### Task 4`。
- [ ] **Worker Task 5：历史回测 handler 注册**  
  计划位置：计划 06 `### Task 1`、`### Task 4`。
- [ ] **Worker Task 6：稳定错误码、重试次数和用户安全错误展示**  
  计划位置：计划 06 `### Task 3`、`### Task 7`。
- [ ] **Worker Task 7：worker lease、stale 自动重入队和跨重启恢复**  
  计划位置：计划 06 `### Task 8`。
- [ ] **Worker Task 8：三类业务任务统一生命周期测试**  
  计划位置：计划 06 `### Task 4`。

## P1：完成严格 PIT 历史回测

对应计划：[计划 04：research backtest](docs/superpowers/plans/2026-07-16-04-backtest-research.md)、[计划 05：strict PIT backtest](docs/superpowers/plans/2026-07-16-05-pit-verified-backtest.md)。

### 已有基础（不代表严格 PIT 完成）

- 基础 ledger、A 股执行尝试、T+1/涨跌停/滑点/费用的部分代码已存在于 `backend/app/features/backtests/`。
- Web 有基础回测页面；严格 PIT 证书和发布门槛尚未接入。

### 待完成 Task

- [ ] **Backtest Task 1：research 回测契约和 fake**  
  计划位置：计划 04 `### Task 1`。
- [ ] **Backtest Task 2：lot-level portfolio ledger 完整验收**  
  计划位置：计划 04 `### Task 2`。
- [ ] **Backtest Task 3：A 股成交限制和执行尝试完整验收**  
  计划位置：计划 04 `### Task 3`。
- [ ] **Backtest Task 4：确定性日事件循环**  
  计划位置：计划 04 `### Task 4`。
- [ ] **Backtest Task 5：A-D 实验隔离**  
  计划位置：计划 04 `### Task 5`。
- [ ] **Backtest Task 6：研究指标和交易明细**  
  计划位置：计划 04 `### Task 6`。
- [ ] **Backtest Task 7：回测结果持久化和 API**  
  计划位置：计划 04 `### Task 7`。
- [ ] **Backtest Task 8：回测 Web 结果和数据等级展示**  
  计划位置：计划 04 `### Task 8`。
- [ ] **Backtest Task 9：research 回测不变量验收**  
  计划位置：计划 04 `### Task 9`。
- [ ] **PIT Task 1-9：canonical bundle、历史表、公开信息选择、复权序列、覆盖证书、毒丸审计、滚动前推和晋级门槛**  
  计划位置：计划 05 `### Task 1` 至 `### Task 9`；当前服务尚未建立完整 PIT verified 发布链。

## P1：历史持仓导入 Web 化

对应计划：计划 01 `### Task 6-9`，运行时代码位于 `backend/app/features/legacy_import/`。

- [x] **Import Task 1：源目录检查和质量标签基础**  
  当前文件：`legacy_import/inspect.py`、`models.py`。
- [x] **Import Task 2：原始字节冻结、SHA-256 和 opening positions CLI 底层**  
  当前文件：`legacy_import/service.py`、`repository.py`、`cli.py`。
- [x] **Import Task 3：CLI 幂等和 DA 独立性基础**  
  计划位置：计划 01 `### Task 9`；仍需最终审计测试。
- [ ] **Import Task 4：Web 导入页面和来源选择**  
  当前缺少对应 Web feature。
- [ ] **Import Task 5：导入预览、质量报告和人工确认**  
  计划位置：计划 01 `### Task 8-9`。
- [ ] **Import Task 6：batch id、manifest hash、导入条数和重复导入展示**  
  计划位置：计划 01 `### Task 8-9`。
- [ ] **Import Task 7：导入后的持仓分析联动验收**  
  依赖 Holding Task 6-10。

## P1：完善数据采集与 provider 组合根

对应计划：计划 01 `### Task 2-5`、计划 06 `### Task 1`。

- [x] **Provider Task 1：PIT 类型、warehouse/source 基础接口**  
  当前文件：`backend/app/core/market/pit_models.py`、`ports/point_in_time.py`、`infrastructure/market/research_warehouse.py`。
- [x] **Provider Task 2：AkShare/BaoStock 行情适配器基础**  
  当前文件：`infrastructure/market/research_providers.py`。
- [ ] **Provider Task 3：行情适配器接入统一生产组合根**  
  当前 `bootstrap/composition.py` 使用 `UnavailableResearchWarehouse`。
- [ ] **Provider Task 4：指数、市场宽度、股票池和行业/主题 PIT 数据**  
  计划位置：计划 01 `### Task 3-5`。
- [ ] **Provider Task 5：官方政策材料及发布日期/available_at 过滤**  
  计划位置：计划 01 `### Task 4`。
- [ ] **Provider Task 6：财报 provider 及公开时间过滤**  
  计划位置：计划 01 `### Task 4`。
- [ ] **Provider Task 7：LLM 因子 schema、证据和禁止交易字段校验**  
  计划位置：计划 01 `### Task 4`；adapter 有基础，但未完成生产接线。
- [ ] **Provider Task 8：provider lineage、manifest hash 和统一点时快照**  
  计划位置：计划 01 `### Task 2-5`。
- [x] **Provider Task 9：生产/fake 模式配置字段**  
  当前 `Settings.provider_mode` 已增加；但 fake harness 和 production provider 仍未完成。
- [ ] **Provider Task 10：provider 故障质量码和 fail-closed 端到端验收**  
  计划位置：计划 02 `### Task 9`、计划 06 `### Task 4`。

## P1：完善运行中心

对应计划：计划 00 `### Task 11`、计划 06 `### Task 3`。

- [x] **Run Center Task 1：任务列表、详情、artifact 基础 API**  
  当前文件：`backend/app/features/runs/router.py`、`service.py`、`artifacts.py`。
- [x] **Run Center Task 2：queued/running/succeeded/failed 基础状态模型**  
  当前文件：`backend/app/contracts/runs.py`、`features/runs/repository.py`。
- [ ] **Run Center Task 3：显示每类业务结果和 artifact 链接**  
  依赖候选/持仓/回测结果 repository 和统一 handler。
- [ ] **Run Center Task 4：stage、progress、heartbeat、错误码和重试次数 Web 展示**  
  计划位置：计划 06 `### Task 3`。
- [ ] **Run Center Task 5：失败任务重试入口和幂等保持**  
  计划位置：计划 06 `### Task 3`。
- [ ] **Run Center Task 6：API/worker 重启后的跨重启状态验收**  
  计划位置：计划 06 `### Task 4`。

## P1：运维、安全与恢复

对应计划：计划 06 `### Task 7-8`。

- [ ] **Ops Task 1：loopback/认证启动策略**  
  当前有基础 CORS/auth middleware，但没有计划中的 fail-fast security validator 和生产认证配置。
- [x] **Ops Task 2：wildcard CORS 基础拒绝和 artifact 路径防穿越基础**  
  当前已有 `create_app` CORS 检查和 `artifact_paths.py`；仍需安全套件验收。
- [ ] **Ops Task 3：worker lease 表和 heartbeat**  
  计划位置：计划 06 `### Task 8`。
- [ ] **Ops Task 4：readiness 检查数据库和 worker 新鲜度**  
  当前 readiness 只有基础探针。
- [ ] **Ops Task 5：stale worker 自动重入队**  
  计划位置：计划 00 `### Task 10`、计划 06 `### Task 8`。
- [x] **Ops Task 6：SQLAlchemy 参数化查询基础**  
  当前 repository 使用 SQLAlchemy 表达式；仍需安全测试确认。
- [ ] **Ops Task 7：结构化日志和敏感字段过滤**  
  计划位置：计划 06 `### Task 7`。
- [ ] **Ops Task 8：数据库备份、artifact hash 校验和恢复 runbook**  
  计划位置：计划 06 `### Task 8`。
- [ ] **Ops Task 9：DA 独立性发布审计**  
  计划位置：计划 06 `### Task 9`。

## P2：自动化测试和发布验收

对应计划：计划 00 `### Task 14`、计划 02-05 各自最后 Task、计划 06 `### Task 4-9`。

- [x] **QA Task 1：现有后端单元测试和前端基础测试**  
  当前已有后端非 PostgreSQL 测试、Vitest、TypeScript 和 Vite build 基线。
- [ ] **QA Task 2：候选推荐完整单元测试**  
  计划位置：计划 02 `### Task 9`；缺 repository/service/jobs/API round-trip 测试。
- [ ] **QA Task 3：持仓分析完整单元测试**  
  计划位置：计划 03 `### Task 10`。
- [ ] **QA Task 4：research/PIT 回测测试**  
  计划位置：计划 04 `### Task 9`、计划 05 `### Task 7-9`。
- [ ] **QA Task 5：PostgreSQL 集成和 worker 生命周期测试**  
  计划位置：计划 06 `### Task 4`。
- [ ] **QA Task 6：API 契约、OpenAPI no-diff 和生成客户端测试**  
  计划位置：计划 06 `### Task 3`、`### Task 5`。
- [ ] **QA Task 7：Playwright 四功能端到端测试**  
  计划位置：计划 06 `### Task 6`。
- [ ] **QA Task 8：安全、发布独立性和审计测试**  
  计划位置：计划 06 `### Task 7`、`### Task 9`。
- [ ] **QA Task 9：CI、Docker、连续两次可复现验收**  
  计划位置：计划 06 `### Task 6-9`。

## 计划总索引

| 计划 | 负责范围 | 当前状态 |
| --- | --- | --- |
| `2026-07-16-00-foundation-contracts.md` | 策略核心、契约、队列、worker 基础、Web 壳、CI 基线 | 基础部分已完成；最终验收未完成 |
| `2026-07-16-01-pit-and-legacy.md` | PIT 类型/source/warehouse、provider adapter、legacy 导入 | 底层部分完成；生产组合和 Web 导入未完成 |
| `2026-07-16-02-candidate-recommendation.md` | 候选模型、编排、持久化、API、worker、Web | Task 1-8 已有主服务实现；Task 9 和真实 provider 验收未完成 |
| `2026-07-16-03-holding-analysis.md` | 持仓建议、持久化、异步 API、worker、Web | 模型/投影部分有实现；Task 5-10 未完成 |
| `2026-07-16-04-backtest-research.md` | research 回测 ledger、执行、指标、API、Web | 基础代码存在；计划验收未完成 |
| `2026-07-16-05-pit-verified-backtest.md` | 严格 PIT bundle、证书、毒丸审计、walk-forward | 尚未完成 |
| `2026-07-16-06-system-integration.md` | 组合根、统一迁移/API、跨功能 worker、Web、运维、发布 | 尚未完成 |
| `2026-07-16-da-multi-agent-execution.md` | 多 agent 波次、依赖、review 和收尾顺序 | 作为调度说明继续使用，不代表功能完成 |

## 推荐调度顺序

1. `[ ]` 先完成计划 06 Task 1 的统一组合根和 provider 模式；不要继续扩展单独的 fail-closed 占位实现。
2. `[ ]` 完成计划 03 Task 5-10，接通持仓分析结果持久化、worker 和 Web。
3. `[ ]` 完成计划 04 Task 7-9，再进入计划 05 的严格 PIT 证书链。
4. `[ ]` 完成计划 01 的 Web legacy 导入，验证 `legacy_opening_balance` 与持仓分析联动。
5. `[ ]` 完成计划 06 Task 3-9：统一 API、worker 生命周期、readiness、lease、安全、Docker、Playwright 和发布审计。
6. `[ ]` 每完成一个 Task，先运行该 Task 计划中的验证命令，再把对应 `[ ]` 改成 `[x]`，记录 commit hash 和验证证据。

## 下次执行入口

```bash
cd /Users/bujiatang/workspace/DA
git status --short
python -m tools.check_openapi
python -m pytest -q -m 'not postgres'
```

然后从“推荐调度顺序”的第一个未勾选任务继续；不要跳过 provider 组合根直接声称候选推荐已完成。
